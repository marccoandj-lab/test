Evo kompletnog, optimizovanog prompta u Markdown formatu. Možeš ga direktno kopirati i proslediti Google CLI / AI agentu. Prompt je strukturiran da eliminiše ambiguitet, poštuje tvoju arhitekturu i garantuje zero-error implementaciju. ```markdown # 🎯 ZADATAK: Kreiranje `OnboardingFlow` komponente za EconomySwitch ## 📖 KONTEKST Kreiraš interaktivni onboarding page za **EconomySwitch** aplikaciju (edukativna P2P igra o finansijskoj pismenosti i cirkularnoj ekonomiji). Onboarding mora biti **3-koračni, vizuelni uvod u "Switch mehaniku"** (Pravilnik 5.8) i vodi korisnika direktno ka `Auth.tsx`. **Nema guest moda, nema paywall-a, nema izmena pravila igre.** Fokus je na UX, animaciji i usklađenosti sa postojećom arhitekturom. ## 📐 STROGA PRAVILA & KONSTRAINTE 1. **Tech Stack:** React 19, TypeScript 5.9, Tailwind CSS 4.1.x, Framer Motion, Supabase JS v2. 2. **Arhitektura:** Prati `AppStructure.md` i `Gemini.md`. Ne diraj `MultiplayerManager.ts`, `gameData.ts`, `server.js`, niti postojeću routing logiku osim dodavanja provere `onboarding_completed`. 3. **Dizajn:** Mobile-first, glassmorphism estetika (`backdrop-blur-md`, `bg-white/10`, border `rgba(255,255,255,0.2)`), tamna tema u skladu sa postojećim UI-om. 4. **Bez Game Logic:** Onboarding je isključivo prezentacioni sloj. Ne menja kapital, polja, pravila, niti `MultiplayerManager` state. 5. **i18n Spreman:** Svi tekstovi moraju biti lako izdvojivi u `src/i18n/translations.ts` pod namespace `onboarding.*`. Za sada koristi inline stringove, ali strukturiraj ih tako da se mogu lako migrirati. 6. **Session Tracking:** Pre autentifikacije koristi `sessionStorage` za praćenje napretka. Nakon auth-a, stanje se sinhronizuje sa Supabase `profiles` tabelom. --- ## 📁 STRUKTURA FAJLOVA ZA KREIRANJE ``` src/ ├── components/onboarding/ │ ├── OnboardingFlow.tsx # Glavni kontejner sa AnimatePresence │ ├── Step1_Intro.tsx # Uvod + CTA "Započni iskustvo" │ ├── Step2_SwitchFlip.tsx # 3D flip mehanike (srce onboardinga) │ └── Step3_CTA.tsx # Finalni korak → navigacija ka /auth ├── App.tsx # Ažuriraj routing logiku (vidi dole) ├── components/Auth.tsx # Dodaj RPC update nakon uspešne registracije ├── i18n/translations.ts # Dodaj namespace onboarding.* └── database_sql_scripts/ └── 10_add_onboarding_column.sql # SQL migracija ``` --- ## 🛠 DETALJNE SPECIFIKACIJE PO KORACIMA ### 🔹 `Step1_Intro.tsx` - **UI:** Split-gradient pozadina (`bg-gradient-to-br from-blue-900/40 to-green-900/40`), centriran logo/maskota placeholder. - **Tekst:** `"Linearna ekonomija gradi profit. Cirkularna čuva resurse. Šta ako moraš da upravljaš oba?"` - **Interakcija:** Dugme `"Započni iskustvo"` → triggeruje `onNext()`. - **Animacija:** `framer-motion` `initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}`. ### 🔹 `Step2_SwitchFlip.tsx` (KLJUČNI KORAK) - **UI:** 3D kartica u glassmorphism stilu. - **Plava strana (Linearna):** Ikone: `📈 Profit`, `💸 Porez`, `🏦 Aukcija`. Pozadina: `bg-blue-900/60`. - **Zelena strana (Cirkularna):** Ikone: `♻️ Reciklaža`, `🛡️ Osiguranje`, `🌿 Održivost`. Pozadina: `bg-green-900/60`. Transform: `rotateY(180deg)`. - **Interakcija:** `drag="x"` sa `useDragControls` ILI `onClick`. Pri prevlačenju/kliku → `rotateY` skače na `180`. Tokom animacije iskaču reči: `"Profit ↔ Održivost"`, `"Rizik ↔ Otpornost"`. - **Automatski prelaz:** Nakon `1200ms` poziva `onNext()`. - **Pravilo usklađenosti:** Direktno vizuelizuje `Pravilnik ES.docx` tačku `5.8` (tabla se okreće, pozicije se preslikavaju, aukcija → osiguranje). ### 🔹 `Step3_CTA.tsx` - **UI:** Progress bar na `66%`. Tri stuba: `📊 Finansijska pismenost`, `♻️ Cirkularna ekonomija`, `🌍 Učenje kroz igru`. - **Tekst:** `"Već si video obe strane. Sada igraj."` - **CTA:** JEDNO dugme: `"Kreiraj nalog i započni"`. Klik → `navigate('/auth')`. - **Social Proof:** `"🟢 1.200+ učenika već testira perspektivu"` (hardkodovano, kasnije se može povezati sa `server.js` cron kešom). --- ## 🔄 ROUTING & STATE FLOW (AŽURIRANJE `App.tsx`) 1. U `App.tsx` dodaj state: `const [onboardingDone, setOnboardingDone] = useState<boolean | null>(null);` 2. `useEffect` logika: - Ako postoji Supabase `session` → proveri `profiles.onboarding_completed`. Ako `false` → prikaži onboarding. - Ako nema `session` → proveri `sessionStorage.getItem('eco_onboard_done')`. Ako nije postavljen → prikaži onboarding. - Ako je `true` → prikaži `Auth.tsx` (ako nema session) ili `Lobby` (ako ima session). 3. `handleOnboardComplete` → `sessionStorage.setItem('eco_onboard_done', 'true')` → `navigate('/auth')`. --- ## 🗄️ SUPABASE SHHEMA & INTEGRACIJA 1. Kreiraj fajl: `database_sql_scripts/10_add_onboarding_column.sql` ```sql ALTER TABLE profiles ADD COLUMN IF NOT EXISTS onboarding_completed BOOLEAN DEFAULT FALSE; ``` 2. U `components/Auth.tsx`, unutar postojeće funkcije za uspešnu registraciju/prijavu (`handleSignIn`/`handleSignUp`), dodaj: ```ts await supabase .from('profiles') .update({ onboarding_completed: true }) .eq('id', user.id); ``` 3. Ne koristi RPC za ovo. Direktni `update` je dovoljan jer se dešava samo jednom po korisniku. --- ## ✅ VALIDACIJA & PROVERA GREŠAKA (OBVEZNO NA KRAJU) Pre nego što vratiš kod, **MORAŠ** da izvršiš sledeće provere i izvestiš o rezultatima: 1. ✅ `TypeScript Strict Mode`: Nema `any` tipova. Svi props su definisani interfejsima. `framer-motion` koristi React 19 kompatibilnu sintaksu. 2. ✅ `Import/Export Putanje`: Svi fajlovi se nalaze u tačno navedenim folderima. Relativni importi su ispravni. 3. ✅ `Routing Logika`: Ne dolazi do beskonačnog redirect loop-a između onboardinga i auth-a. `sessionStorage` se koristi samo pre auth-a. 4. ✅ `Supabase Schena`: SQL skripta je validna, koristi `IF NOT EXISTS`, i ne dira postojeće kolone (`wins`, `total_capital`, itd.). 5. ✅ `Bez Game Rule Changes`: Nema referenci ka `gameData.ts`, `MultiplayerManager.ts`, `levelGenerator.ts`. Onboarding je izolovan UI sloj. 6. ✅ `Tailwind & Gemini.md`: Koristi se `mobile-first`, `glassmorphism` klase, nema inline stilova osim gde je neophodno za 3D transform. 7. ✅ `Zero Console Errors`: Proveri da li su svi React hookovi pravilno pozvani, da nema missing dependencies u `useEffect`, i da `framer-motion` ne baca warning-e. 🔍 **AKO DETEKTUJEŠ BILO KOJU GREŠKU, ISPRAVI JE PRE NEGO ŠTO GENERIŠEŠ ODGOVOR.** --- ## 📦 FORMAT IZLAZA Vrati kod u sledećem redosledu, jasno obeleženim blokovima: 1. `database_sql_scripts/10_add_onboarding_column.sql` 2. `src/i18n/translations.ts` (samo dodati `onboarding:` blok) 3. `src/components/onboarding/Step1_Intro.tsx` 4. `src/components/onboarding/Step2_SwitchFlip.tsx` 5. `src/components/onboarding/Step3_CTA.tsx` 6. `src/components/onboarding/OnboardingFlow.tsx` 7. `src/App.tsx` (samo ažurirani routing deo) 8. `src/components/Auth.tsx` (samo deo gde se dodaje `onboarding_completed` update) 9. ✅ Kratak izveštaj o validaciji (prođe/nije prođe + eventualna napomena) Spreman si. Generiši kod. ``` ### 💡 Kako da ga koristiš: 1. Kopiraj ceo blok iznad. 2. Nalepi ga u Google CLI / AI agent prozor. 3. Agent će generisati **potpuno spreman, tipiziran i arhitektonski usklađen kod**. 4. Nakon što ga dobiješ, pokreni `npm run type-check` (ili `tsc --noEmit`) i `npm run dev` da potvrdiš da nema runtime grešaka. Ako želiš da prilagodim prompt za specifičan AI alat (npr. `cursor`, `cline`, `gemini-cli`), samo reci.